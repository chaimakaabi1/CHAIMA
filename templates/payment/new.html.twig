{% extends 'base.html.twig' %}

{% block content %}
    {% block title %}New Payment{% endblock %}

    {% block body %}
        <div class="container mt-4">
            <div class="row">
                <div class="col-md-8 offset-md-2">
                    <div class="card">
                        <div class="card-header">
                            <h1 class="mb-0">Create a New Payment</h1>
                        </div>
                        <div class="card-body">
                            {{ include('payment/_form.html.twig') }}
                        </div>
                        <div class="card-footer">
                            <a href="{{ path('app_payment_index') }}" class="btn btn-secondary">Back to List</a>
                            <a href="{{ path('app_abonnement_new') }}" class="btn btn-danger">Cancel</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endblock %}
{% endblock %}
